/etc/hsi-config - configuration files and parameters for system control

system services, functions and utilities will reference the values in this folder for operation

<filename> = has a raw value that can be easily read (e.g. cat <filename> )

<filename>.config = has a #-commented list of parameters, as PARAMETER_NAME <parameter-value>